# Read a csv file and add the filename as a column

Read a csv file and add the filename as a column

## Usage

``` r
read_csv_with_filename(filepath)
```

## Arguments

- filepath:

  Filepath to a csv file.

## Value

The csv file as a dataframe with the filename and filepath as additional
columns
